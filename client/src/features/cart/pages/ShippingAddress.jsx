import React from 'react'
import { useCart } from '../hooks/useCart';

const ShippingAddress = () => {

const { handleAddToCartOrder } = useCart();

    
  return (
    <div>ShippingAddress</div>
  )
}

export default ShippingAddress